# Decrypted/plaintext keytabs and any local working files used to
# generate them — never commit. Only the `.age`-encrypted form belongs
# in this repo.
keytab-*
!keytab-*.age
*.keytab
*.dec
krb5.conf
kdc.conf

# Nix dev tooling artifacts — matches nixie's .gitignore
/.direnv
/result
/.pre-commit-config.yaml
/.claude
